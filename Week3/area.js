// area is the identifier/name
// width and height are received parameters
function area(width, height) {
  return width * height;
}

// 10 and 20 are arguments being passed
console.log(area(10, 20));
console.log(area(12, 20));
console.log(area(24, 40));

// height = 10;
// width = 20;
// myArea = area(width, height);

// console.log(myArea);
